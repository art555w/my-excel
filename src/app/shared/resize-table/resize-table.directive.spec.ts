import { ResizeTableDirective } from './resize-table.directive';

describe('ResizeTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeTableDirective();
    expect(directive).toBeTruthy();
  });
});
