import { SelectCellDirective } from './select-cell.directive';

describe('SelectCellDirective', () => {
  it('should create an instance', () => {
    // const directive = new SelectCellDirective();
    // expect(directive).toBeTruthy();
  });
});
