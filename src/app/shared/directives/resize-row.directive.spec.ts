import { ResizeRowDirective } from './resize-row.directive';

describe('ResizeRowDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeRowDirective();
    expect(directive).toBeTruthy();
  });
});
