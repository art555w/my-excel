import { MousedownTableDirective } from './mousedown-table.directive';

describe('MousedownTableDirective', () => {
  it('should create an instance', () => {
    const directive = new MousedownTableDirective();
    expect(directive).toBeTruthy();
  });
});
