import { ResizeColDirective } from './resize-col.directive';

describe('ResizeColDirective', () => {
  it('should create an instance', () => {
    // const directive = new ResizeColDirective();
    // expect(directive).toBeTruthy();
  });
});
