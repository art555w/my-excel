import { SelectByKeyDirective } from './select-by-key.directive';

describe('SelectByKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectByKeyDirective();
    expect(directive).toBeTruthy();
  });
});
