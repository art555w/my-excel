import { GetTitleDirective } from './get-title.directive';

describe('GetTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new GetTitleDirective();
    expect(directive).toBeTruthy();
  });
});
