import { InitCellsDirective } from './init-cells.directive';

describe('InitCellsDirective', () => {
  it('should create an instance', () => {
    const directive = new InitCellsDirective();
    expect(directive).toBeTruthy();
  });
});
