import { KeydownTableDirective } from './keydown-table.directive';

describe('KeydownTableDirective', () => {
  it('should create an instance', () => {
    const directive = new KeydownTableDirective();
    expect(directive).toBeTruthy();
  });
});
